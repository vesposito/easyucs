{
   "id": "intersight_config_orgs_thermal_policies#",
   "$schema": "http://json-schema.org/draft-07/schema#",
   "title": "EasyUCS intersight config orgs thermal_policies section",
   "description": "EasyUCS intersight config orgs thermal_policies section JSON schema",
   "type": "object",
   "properties": {
      "comment": {
         "description": "Thermal Policy Comments",
         "type": "string",
         "default": ""
      },
      "descr": {
         "description": "Thermal Policy Description",
         "type": "string",
         "default": "",
         "pattern": "^$|^[a-zA-Z0-9]+[\\x00-\\xFF]*$",
         "maxLength": 1024
      },
      "name": {
         "description": "Thermal Policy Name",
         "type": "string",
         "default": "",
         "pattern": "^[a-zA-Z0-9_.:-]{1,64}$"
      },
      "tags": {
         "description": "Thermal Policy Tags",
         "type": "array",
         "items": {
            "description": "Thermal Policy Tag",
            "type": "object",
            "properties": {
               "key": {
                  "description": "Thermal Policy Tag Key",
                  "type": "string",
                  "default": "",
                  "minLength": 1,
                  "maxLength": 128
               },
               "value": {
                  "description": "Thermal Policy Tag Value",
                  "type": "string",
                  "default": "",
                  "minLength": 0,
                  "maxLength": 256
               }
            },
            "additionalProperties": false,
            "required": [
               "key"
            ]
         },
         "minItems": 1
      },
      "fan_control_mode": {
         "description": "Sets the Fan Control Mode of the System. High Power, Maximum Power and Acoustic modes are only supported for Cisco UCS X series Chassis. * `Balanced` - The fans run faster when needed based on the heat generated by the server. When possible, the fans returns to the minimum required speed. * `LowPower` - The Fans run at the minimum speed required to keep the server cool. * `HighPower` - The fans are kept at higher speed to emphasizes performance over power consumption. This Mode is only supported for UCS X series Chassis. * `MaximumCooling` - Operates fans at full speed to provide maximum cooling. Supported on Cisco UCS C-Series M7 and above servers in both Intersight Managed Mode and Intersight Standalone Mode (firmware 4.3(5.250001) and later).  * `MaximumPower` - The fans are always kept at maximum speed. This option provides the most cooling and consumes the most power. This Mode is only supported for UCS X series Chassis. * `Acoustic` - The fan speed is reduced to reduce noise levels in acoustic-sensitive environments. This Mode is only supported for UCS X series Chassis.",
         "type": "string",
         "default": "Balanced",
         "enum": [
            "Balanced",
            "Low Power",
            "High Power",
            "Maximum Cooling",
            "Maximum Power",
            "Acoustic"
         ]
      }
   },
   "additionalProperties": false,
   "required": [
      "name"
   ]
}