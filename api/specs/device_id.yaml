type: object
required:
    - password
properties:
    username:
        type: string
        description: 'Username/Login of the device'
        example: admin
    password:
        type: string
        description: 'Password of the device'
        example: password
