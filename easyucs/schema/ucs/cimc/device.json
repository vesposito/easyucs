{
   "id": "ucs_cimc_device#",
   "$schema": "http://json-schema.org/draft-07/schema#",
   "title": "EasyUCS cimc device section",
   "description": "EasyUCS cimc device section JSON schema",
   "type": "object",
   "properties": {
      "target":{
         "description":"The UCS cimc Device IP or hostname targeted",
         "type":"string"
      },
      "username": {
         "description": "User Name to use for the UCS cimc Device",
         "type": "string"
      },
      "password": {
         "description": "Password of the User to use for the UCS cimc Device",
         "type": "string"
      }
   },
   "required": [
      "target",
      "username",
      "password"
   ],
   "additionalProperties": false
}